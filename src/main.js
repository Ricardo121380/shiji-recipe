const icons={book:'<path d="M4 4h6a3 3 0 0 1 3 3v14a4 4 0 0 0-4-3H4z"/><path d="M20 4h-4a3 3 0 0 0-3 3v14a4 4 0 0 1 4-3h3z"/>',plus:'<path d="M12 5v14M5 12h14"/>',search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 4 4"/>',heart:'<path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.7l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 21l8.8-8.6a5.5 5.5 0 0 0 0-7.8Z"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',edit:'<path d="m16 3 5 5-12 12-6 1 1-6zM14 5l5 5"/>',arrow:'<path d="M5 12h14m-5-5 5 5-5 5"/>',grid:'<rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>',leaf:'<path d="M20 3C7 2 2 8 5 15s16 6 15-12Z"/><path d="m4 21 11-12"/>',upload:'<path d="M12 16V3m-5 5 5-5 5 5M4 15v6h16v-6"/>',check:'<path d="m5 12 4 4L19 6"/>',close:'<path d="m6 6 12 12M6 18 18 6"/>',people:'<circle cx="9" cy="8" r="3"/><path d="M3 21v-3a6 6 0 0 1 12 0v3M16 5a3 3 0 0 1 0 6m2 4a5 5 0 0 1 3 5"/>',print:'<path d="M6 8V3h12v5M6 17H3V9h18v8h-3M6 14h12v7H6z"/>'};
const ico=n=>`<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.65" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[n]||icons.book}</svg>`;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const photo=(id)=>`https://images.unsplash.com/${id}?auto=format&fit=crop&w=1000&q=85`;
const seed=[{id:'1',name:'番茄罗勒意面',category:'主食',time:25,servings:2,description:'酸甜的番茄遇上新鲜罗勒，把简单的一餐，做得有滋有味。',image:photo('photo-1473093226795-af9932fe5856'),ingredients:'意大利面 200g\n樱桃番茄 200g\n新鲜罗勒 适量\n大蒜 3瓣\n橄榄油 2勺\n盐、黑胡椒 适量',steps:['锅中加入足量清水和一小勺盐，煮沸后放入意大利面，按包装时间煮至自己喜欢的软硬度。留半碗煮面水备用。','番茄洗净对半切，大蒜切片。平底锅倒入橄榄油，小火炒香蒜片，加入番茄翻炒至变软出汁。','加入意面和少量煮面水，翻拌均匀。用盐和黑胡椒调味，最后放入新鲜罗勒，装盘即可。'],favorite:true,menu:true},{id:'2',name:'牛油果鲜虾沙拉',category:'轻食',time:15,servings:1,description:'一碗清爽，也是一份认真照顾自己的心意。',image:photo('photo-1512621776951-a57141f2eefd'),ingredients:'牛油果 1个\n虾仁 100g\n生菜 适量\n小番茄 6个\n柠檬 半个',steps:['洗净蔬菜并沥干，牛油果切片，小番茄对半切。','虾仁煮熟，和蔬菜放入碗中。','加入橄榄油、柠檬汁和少许盐，轻轻拌匀。'],favorite:false,menu:true},{id:'3',name:'香煎三文鱼',category:'家常菜',time:20,servings:2,description:'外皮微脆，内里柔嫩，简单调味就很美味。',image:photo('photo-1467003909585-2f8a72700288'),ingredients:'三文鱼 300g\n柠檬 半个\n芦笋 6根\n盐、黑胡椒 适量',steps:['三文鱼擦干水分，两面撒盐和黑胡椒。','平底锅加油，鱼皮朝下煎至金黄，翻面继续煎熟。','芦笋煎熟配在旁边，挤上柠檬汁。'],favorite:true,menu:false},{id:'4',name:'周末松饼',category:'烘焙甜点',time:30,servings:2,description:'慢一点的早晨，从一份热乎乎的松饼开始。',image:photo('photo-1528207776546-365bb710ee93'),ingredients:'低筋面粉 150g\n鸡蛋 1个\n牛奶 150ml\n泡打粉 4g\n蜂蜜 适量',steps:['面粉、泡打粉过筛，与鸡蛋和牛奶混合成面糊。','不粘锅小火预热，倒入面糊，表面冒泡后翻面。','煎至两面金黄，搭配水果和蜂蜜。'],favorite:false,menu:false}];
let recipes;try{const data=JSON.parse(localStorage.getItem('shiji-recipes-v1'));recipes=Array.isArray(data)?data:structuredClone(seed)}catch{recipes=structuredClone(seed)}
let selected=recipes[0]?.id,view='all',category='全部',query='',editing=null,draftImage='',dirty=false;
const categories=['家常菜','主食','轻食','汤羹','烘焙甜点','饮品'];
function toast(text){document.querySelector('#toast').textContent=text;document.querySelector('#toast').classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>document.querySelector('#toast').classList.remove('show'),2800)}
function save(){try{localStorage.setItem('shiji-recipes-v1',JSON.stringify(recipes));return true}catch{toast('保存失败：浏览器空间不足，请减少图片大小或导出备份。');return false}}
function mutate(fn){const old=structuredClone(recipes);fn();if(!save()){recipes=old;return false}return true}
function filtered(){return recipes.filter(r=>(view!=='favorites'||r.favorite)&&(view!=='menu'||r.menu)&&(category==='全部'||r.category===category)&&[r.name,r.description,r.ingredients].join(' ').toLowerCase().includes(query.toLowerCase()))}
function render(){const list=filtered();if(!list.some(r=>r.id===selected))selected=list[0]?.id;const r=recipes.find(x=>x.id===selected);document.querySelector('#app').innerHTML=`<aside class="sidebar"><a class="brand" href="/"> <span class="brand-symbol">${ico('leaf')}</span><span>食记<span class="brand-en">SHIJI / RECIPE JOURNAL</span></span></a><div class="space-label">我的厨房</div><nav>${[['all','book','全部菜谱',recipes.length],['favorites','heart','我的收藏',recipes.filter(r=>r.favorite).length],['menu','grid','今日菜单',recipes.filter(r=>r.menu).length]].map(([id,icon,label,n])=>`<button class="nav-item ${view===id?'active':''}" data-view="${id}">${ico(icon)}<span>${label}</span><span class="count">${n}</span></button>`).join('')}</nav><div class="sidebar-note"><span class="little-sprig">${ico('leaf')}</span><strong>好好吃饭，好好生活。</strong><p>把喜欢的味道记下来，<br>让每一餐都有迹可循。</p></div><div class="sidebar-bottom"><span class="avatar">我</span><div>我的私人菜谱<small>保存在当前浏览器</small></div><span class="status-dot"></span></div></aside><div class="workspace"><header class="topbar"><div class="breadcrumb">我的厨房 <span>/</span> ${view==='favorites'?'我的收藏':view==='menu'?'今日菜单':'全部菜谱'}</div><div class="top-actions"><button class="text-button" id="export">${ico('upload')}<span>导出备份</span></button><span class="top-divider"></span><span class="local-status">${ico('check')} 本地保存</span></div></header><main><section class="intro"><div><div class="eyebrow">A LITTLE KITCHEN, A LOT OF LOVE</div><h1>${view==='favorites'?'总有些味道，值得偏爱。':view==='menu'?'今天，吃点喜欢的。':'把好味道，留下来。'}</h1><p>${view==='menu'?'从菜谱里挑选喜欢的菜，组成今天的小小菜单。':'记录拿手好菜，收藏日常灵感，慢慢写成自己的食谱。'}</p></div><button class="primary" id="add">${ico('plus')} 新建菜谱</button></section><div class="collection-bar"><div class="collection-title"><h2>${view==='favorites'?'我的收藏':view==='menu'?'今日菜单':'我的菜谱'}</h2><span>${list.length} 道用心记录</span></div><div class="collection-tools">${view==='menu'?`<button class="text-button" id="preview">${ico('book')} 预览菜单</button>`:''}<label class="search">${ico('search')}<input id="search" placeholder="搜索菜名、食材…" value="${esc(query)}" aria-label="搜索菜名或食材"><kbd>⌕</kbd></label></div></div><div class="filters">${['全部',...categories].map(c=>`<button data-category="${c}" class="filter ${category===c?'chosen':''}">${c}${c==='全部'?`<span>${recipes.filter(r=>view==='all'||(view==='favorites'?r.favorite:r.menu)).length}</span>`:''}</button>`).join('')}</div><div class="recipe-layout"><section class="cards" aria-label="菜谱列表">${list.map(card).join('')||`<div class="empty"><div>${ico('book')}</div><h3>${query?'没有找到这道菜':'这里还没有菜谱'}</h3><p>${query?'换个菜名或食材试试。':view==='menu'?'在菜谱详情中点击「加入菜单」。':view==='favorites'?'点击菜谱上的爱心，收藏喜欢的味道。':'点击「新建菜谱」，记录第一道好菜。'}</p></div>`}${list.length?`<button class="new-card" id="add-card"><span>${ico('plus')}</span><strong>下一道拿手菜</strong><small>把新的美味灵感记下来</small></button>`:''}</section><aside class="detail">${r?detail(r):`<div class="detail-empty">${ico('leaf')}<p>每道好菜，都值得记下来。</p></div>`}</aside></div><footer>一蔬一饭，皆是生活。<span>用心记录 · 慢慢积累</span></footer></main></div><dialog id="editor"></dialog><dialog id="menu-preview"></dialog>`;bind()}
function card(r){return `<article class="recipe-card ${selected===r.id?'selected':''}"><button class="card-main" data-select="${r.id}" aria-label="查看${esc(r.name)}"><div class="card-image">${r.image?`<img src="${esc(r.image)}" alt="${esc(r.name)}" loading="lazy" onerror="this.style.display='none'">`:''}<span class="image-placeholder">${ico('leaf')}</span><span class="category-badge">${esc(r.category)}</span></div><div class="card-content"><h3>${esc(r.name)}</h3><p>${esc(r.description||'一道值得记录的好味道')}</p><div class="card-meta"><span>${ico('clock')} ${esc(r.time)} 分钟</span><span>${ico('people')} ${esc(r.servings)} 人份</span>${selected===r.id?`<span class="viewing">正在查看 ${ico('arrow')}</span>`:''}</div></div></button><button class="favorite ${r.favorite?'is-favorite':''}" data-favorite="${r.id}" aria-label="${r.favorite?'取消收藏':'收藏'}${esc(r.name)}" aria-pressed="${r.favorite}">${ico('heart')}</button></article>`}
function detail(r){return `<div class="detail-heading"><span>菜谱详情</span><button class="text-button" id="edit">${ico('edit')} 编辑菜谱</button></div><div class="detail-image">${r.image?`<img src="${esc(r.image)}" alt="${esc(r.name)}" onerror="this.style.display='none'">`:''}<span class="image-placeholder">${ico('leaf')}</span><span class="detail-image-label">FROM MY KITCHEN</span></div><div class="detail-body"><div class="detail-tags"><span>${esc(r.category)}</span><span>${ico('clock')} ${esc(r.time)} 分钟</span><span>${ico('people')} ${esc(r.servings)} 人份</span></div><h2>${esc(r.name)}</h2><p class="description">${esc(r.description||'记录美味，也记录生活。')}</p><section class="ingredients"><h3>${ico('leaf')} 准备食材 <small>${esc(r.servings)} 人份</small></h3><div class="ingredient-list">${r.ingredients.split('\n').filter(x=>x.trim()).map(x=>`<div>${esc(x)}</div>`).join('')||'<p class="muted">还没有填写食材</p>'}</div></section><section class="steps"><h3>${ico('book')} 开始烹饪 <small>${r.steps.length} 个步骤</small></h3>${r.steps.map((s,i)=>`<div class="step"><span>${String(i+1).padStart(2,'0')}</span><p>${esc(s)}</p></div>`).join('')}</section><button class="menu-toggle ${r.menu?'added':''}" id="menu-toggle">${ico(r.menu?'check':'plus')} ${r.menu?'已加入今日菜单 · 点击移除':'加入今日菜单'}</button></div>`}
function bind(){document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{view=b.dataset.view;category='全部';query='';render()});document.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>{category=b.dataset.category;render()});document.querySelectorAll('[data-select]').forEach(b=>b.onclick=()=>{selected=b.dataset.select;render()});document.querySelectorAll('[data-favorite]').forEach(b=>b.onclick=()=>{mutate(()=>{const r=recipes.find(x=>x.id===b.dataset.favorite);r.favorite=!r.favorite});render()});document.querySelector('#search').oninput=e=>{const pos=e.target.selectionStart;query=e.target.value;render();const input=document.querySelector('#search');input.focus();input.setSelectionRange(pos,pos)};document.querySelector('#add').onclick=()=>openEditor();document.querySelector('#add-card')?.addEventListener('click',()=>openEditor());document.querySelector('#edit')?.addEventListener('click',()=>openEditor(recipes.find(x=>x.id===selected)));document.querySelector('#menu-toggle')?.addEventListener('click',()=>{if(mutate(()=>{const r=recipes.find(x=>x.id===selected);r.menu=!r.menu})){render();toast('今日菜单已更新')}});document.querySelector('#export').onclick=()=>{const blob=new Blob([JSON.stringify({version:1,recipes},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='食记-菜谱备份.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);toast('菜谱备份已导出')};document.querySelector('#preview')?.addEventListener('click',preview)}
function openEditor(r){editing=r?.id||null;draftImage=r?.image||'';dirty=false;const dialog=document.querySelector('#editor');dialog.innerHTML=`<form id="recipe-form"><div class="modal-heading"><div><span class="eyebrow">YOUR NEXT SIGNATURE DISH</span><h2>${r?'编辑菜谱':'记录一道新菜'}</h2></div><button type="button" class="icon-button" id="close-editor" aria-label="关闭编辑">${ico('close')}</button></div><div class="editor-content"><label class="field">菜品名称 <span class="required">*</span><input name="name" required maxlength="60" placeholder="例如：妈妈的红烧肉" value="${esc(r?.name||'')}"></label><div class="field-row"><label class="field">分类<select name="category">${categories.map(c=>`<option ${r?.category===c?'selected':''}>${c}</option>`).join('')}</select></label><label class="field">用时（分钟）<input name="time" type="number" min="1" max="9999" required value="${r?.time||30}"></label><label class="field">人份<input name="servings" type="number" min="1" max="100" required value="${r?.servings||2}"></label></div><label class="field">一句话介绍<input name="description" maxlength="180" placeholder="这道菜，有什么特别的故事？" value="${esc(r?.description||'')}"></label><div class="field">菜品配图 <span class="optional">选填 · JPG、PNG、WebP，最大 10MB</span><label class="upload-area" for="image-input"><img id="image-preview" alt="菜品图片预览" ${draftImage?`src="${esc(draftImage)}"`:'hidden'}><span>${ico('upload')}<strong>点击上传菜品照片</strong><small>上传后自动优化尺寸</small></span></label><input type="file" id="image-input" accept="image/jpeg,image/png,image/webp" hidden><button type="button" id="remove-image" class="text-button" ${draftImage?'':'hidden'}>移除图片</button></div><label class="field">准备食材 <span class="optional">每行一种食材及用量</span><textarea name="ingredients" rows="4" placeholder="五花肉 500g\n生姜 3片\n生抽 2勺">${esc(r?.ingredients||'')}</textarea></label><div class="field">做法教程 <span class="required">*</span><div id="step-fields">${(r?.steps||['']).map((s,i)=>stepField(s,i)).join('')}</div><button class="add-step" type="button" id="add-step">${ico('plus')} 添加步骤</button></div><p class="form-error" id="form-error" role="alert"></p></div><div class="modal-footer">${r?'<button type="button" class="danger-button" id="delete-recipe">删除菜谱</button>':'<span class="muted">好味道，从这一次记录开始。</span>'}<div><button type="button" class="secondary" id="cancel-editor">取消</button><button type="submit" class="primary" id="save-recipe">${ico('check')} 保存菜谱</button></div></div></form>`;dialog.showModal();const close=()=>{if(!dirty||confirm('尚未保存的内容将丢失，确定关闭？'))dialog.close()};dialog.oncancel=e=>{e.preventDefault();close()};document.querySelector('#close-editor').onclick=close;document.querySelector('#cancel-editor').onclick=close;document.querySelector('#recipe-form').oninput=()=>dirty=true;document.querySelector('#add-step').onclick=()=>{const fields=document.querySelector('#step-fields');fields.insertAdjacentHTML('beforeend',stepField('',fields.children.length));dirty=true;bindStepRemove()};bindStepRemove();document.querySelector('#remove-image').onclick=()=>{draftImage='';document.querySelector('#image-preview').hidden=true;document.querySelector('#image-input').value='';document.querySelector('#remove-image').hidden=true;dirty=true};document.querySelector('#image-input').onchange=async e=>{const file=e.target.files[0];if(!file)return;if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>10*1024*1024){toast('请选择 10MB 以内的 JPG、PNG 或 WebP 图片');return}const button=document.querySelector('#save-recipe');button.disabled=true;try{draftImage=await compressImage(file);const img=document.querySelector('#image-preview');img.src=draftImage;img.hidden=false;document.querySelector('#remove-image').hidden=false;dirty=true}catch{toast('图片读取失败，请换一张图片重试')}finally{button.disabled=false}};document.querySelector('#delete-recipe')?.addEventListener('click',()=>{if(confirm(`确定删除「${r.name}」？此操作无法撤销。`)&&mutate(()=>{recipes=recipes.filter(x=>x.id!==r.id)})){dialog.close();render();toast('菜谱已删除')}});document.querySelector('#recipe-form').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target);const name=data.get('name').trim(),steps=[...document.querySelectorAll('.step-input textarea')].map(x=>x.value.trim()).filter(Boolean);if(!name||!steps.length){document.querySelector('#form-error').textContent='请填写菜品名称和至少一个做法步骤。';return}const next={id:editing||crypto.randomUUID(),name,category:data.get('category'),time:Number(data.get('time')),servings:Number(data.get('servings')),description:data.get('description').trim(),ingredients:data.get('ingredients').trim(),image:draftImage,steps,favorite:r?.favorite||false,menu:r?.menu||false};if(mutate(()=>{if(editing)recipes=recipes.map(x=>x.id===editing?next:x);else recipes.unshift(next)})){selected=next.id;view='all';category='全部';query='';dialog.close();render();toast('菜谱已保存')}}}
function stepField(s,i){return `<div class="step-input"><span>${String(i+1).padStart(2,'0')}</span><textarea aria-label="步骤 ${i+1}" rows="3" placeholder="写下这一步的做法…">${esc(s)}</textarea><button type="button" class="icon-button remove-step" aria-label="删除步骤 ${i+1}">${ico('close')}</button></div>`}
function bindStepRemove(){document.querySelectorAll('.remove-step').forEach(b=>b.onclick=()=>{if(document.querySelector('#step-fields').children.length===1){toast('请至少保留一个步骤');return}b.closest('.step-input').remove();[...document.querySelector('#step-fields').children].forEach((row,i)=>{row.querySelector('span').textContent=String(i+1).padStart(2,'0');row.querySelector('textarea').setAttribute('aria-label',`步骤 ${i+1}`);row.querySelector('button').setAttribute('aria-label',`删除步骤 ${i+1}`)});dirty=true})}
async function compressImage(file){const bitmap=await createImageBitmap(file);const ratio=Math.min(1,1400/Math.max(bitmap.width,bitmap.height));const canvas=document.createElement('canvas');canvas.width=Math.round(bitmap.width*ratio);canvas.height=Math.round(bitmap.height*ratio);canvas.getContext('2d').drawImage(bitmap,0,0,canvas.width,canvas.height);bitmap.close();return canvas.toDataURL('image/webp',0.8)}
function preview(){const dialog=document.querySelector('#menu-preview');const list=recipes.filter(r=>r.menu);dialog.innerHTML=`<div class="menu-paper"><button class="icon-button print-hide" id="close-preview" aria-label="关闭预览">${ico('close')}</button><span class="eyebrow">TODAY'S TABLE</span><h2>今日菜单</h2><p class="menu-date">${new Date().toLocaleDateString('zh-CN',{year:'numeric',month:'long',day:'numeric'})}</p>${list.map(r=>`<div class="menu-line"><div><span>${esc(r.category)}</span><h3>${esc(r.name)}</h3><p>${esc(r.description)}</p></div><small>${esc(r.servings)} 人份</small></div>`).join('')||'<p>还没有选好的菜品，先在菜谱详情中加入菜单吧。</p>'}<p class="menu-signature">好好吃饭，好好生活。<br><small>食记 · SHIJI</small></p><button class="primary print-hide" id="print-menu">${ico('print')} 打印 / 保存为 PDF</button></div>`;dialog.showModal();document.querySelector('#close-preview').onclick=()=>dialog.close();document.querySelector('#print-menu').onclick=()=>window.print()}
render();
