# 2026-09-12

## 饭Fun 项目只读架构评估（未改动任何代码）

方式：通读 src/、functions/、schema/、scripts/、部署配置与 git log，并在临时目录（/tmp）跑一次构建验证，未写入项目文件。

体量：源码 2,492 行（src 约 1,520 行，functions 约 747 行）；生产零运行时依赖，仅 vite devDependency；构建产物 JS 149KB / gzip 46KB，CSS 32KB / gzip 7.3KB；13 个模块。

评估结论：架构取舍合理（本地优先 + 配图内容寻址 + 保守同步），主要短板在安全限流、文档一致性、工程化。

发现（按优先级，均为待办，未实施）：
1. `/api/sync/{code}`（含配图端点）三个入口全无限流，同步码即唯一凭证，可被枚举读取/覆盖；账号路径有 rateLimit，码路径没有。auth.js 已有 rateLimit 工具可复用。
2. PBKDF2-SHA256 迭代 80,000，低于 OWASP 现行建议；会话 token 明文存 D1、有效期 30 天。
3. README 严重过期：仍写「烹饪菜 + 零食速食两个板块」「云同步用 KV」，与现状（菜单/水吧、D1+R2+账号）不符。
4. settings.js 顶部注释写「四组」，实际 GROUPS 有 7 组。
5. 循环依赖 recipes.js ⇄ kitchen.js（函数内运行时调用，可运行但不健康）；main.js 导入未使用的 setType；ui.js 动态 import store.js 触发 Vite 警告。
6. 遗留物：`S.log` 全仓无引用但仍列入 syncdb 的 DOC_KEYS 参与同步；`favorite` 只存不展示；index.html 的 `window.onerror → sessionStorage.__err` 无人读取。
7. `npm run check` 有 5 条误报（`#sync-unbind` 里的 unbind、ui.js 的动态 import toast 未被脚本识别）；CI 只跑 build，未跑 check 与 test-syncdb。
8. 样式：仅 4 条媒体查询、6 个 CSS 变量、无深色模式；无 manifest/favicon。

保留未动的约定：仓库名、CF 项目名、`base:'./'`、`shiji-recipes-v1` 只读旧键 —— 评估全程未触碰。
