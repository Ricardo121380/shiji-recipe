# 饭Fun · 厨房伴侣（原名：食记、饭饭）

「饭Fun」是一个轻量的中文厨房管理网页：菜谱点菜、本周菜单、冰箱库存、日用品、热量统计，纯前端 + 浏览器本地存储，无需服务器。

**线上地址（已固定，长期不变）：**

- **GitHub Pages（主地址，分享给他人用这个）**：https://ricardo121380.github.io/shiji-recipe/
- Cloudflare Pages（备用线路）：https://shiji-recipe.pages.dev/

两条线路网址长期固定，后续更新只替换内容、不改网址。菜谱数据按「网址 + 浏览器」隔离，请固定使用同一网址访问；跨线路搬家用「导出备份 / 导入备份」。详见 [AGENTS.md](AGENTS.md) 中的网址与数据约定。

## 功能

- **菜谱**：烹饪菜与零食速食两个板块；1:1 卡片配图；用时（小时/分钟）、人份、热量；食材按「名称 + 用量」结构化记录；步骤三行式呈现（提前准备标识 / 文字 / 配图），可勾选「需要提前准备」；分类与「规格」（口味、忌口等）完全自定义。
- **点菜**：菜谱、零食、外出餐都能点——选份数、规格（口味/忌口等）和就餐日期，自动排进本周菜单。
- **本周菜单**：7 天 × 早/午/晚/加餐；可手动更改份数、规格、更换菜品；**勾选「已吃」才扣库存、记热量，取消勾选自动回补**；含「提前准备」步骤的菜品会标注并在前一天 18:00 通知（页面打开时）。
- **冰箱**：食材 / 速食零食两个板块（零食可标注品牌、口味）；保质期四档筛选（全部 / 新鲜 >7 天 / 快过期 1-7 天 / 已过期）；临期预警 + 浏览器通知；猫 / 狗能否吃分别标注；「最近添加」一键带出；**多选食材找菜谱**：优先推荐包含全部所选的菜谱，其余部分匹配排后，参考剩余数量。
- **购买清单**：按本周菜单（含份数）自动算缺口，按食材分类打标签可筛选；手动添加、编辑、已完成自动沉底；日用品低库存一键加入。
- **日用品库存**：与冰箱类似的库存管理（洗衣液、厨房纸等），分类可自定义，低库存提醒并可加入购买清单。
- **热量记录**：自定义每日目标；数据以本周菜单勾选「已吃」为准（按份数累计），取消勾选自动扣除。
- **菜品推荐**：根据冰箱剩余量、临期食材、今日热量余量推荐，1:1 图片展示。
- **就餐记录**：按自然月保存吃过的每一餐（含份数与规格），月初可复制或邮件发送上月清单。
- **外出就餐**：记录店名/品牌、分类（面食、火锅、饮品等，可自定义）、热量与用时，可点进菜单。
- **云同步（可选）**：顶栏「云同步」生成同步码绑定 Cloudflare KV，多设备自动同步（打开时/改动后/每 30 秒检查，冲突时二选一）；未绑定则纯本地。数据仍可用导出 / 导入 JSON 备份。

## 启动

```bash
npm install
npm run dev -- --port 5173        # 本地开发
npm run build                     # 生产构建 → dist/
npm run deploy                    # 构建并发布到 Cloudflare Pages
```

## 说明与限制

- 通知基于浏览器 Notification API：页面打开时检查（临期提醒、前一天 18:00 备菜提醒）；iOS 需将网页添加到主屏幕。
- 邮件发送通过 `mailto:` 调起本机邮件应用，静态网页无法后台直接发送邮件。
- 图片上传自动压缩（主图最长边 1400px、步骤图 800px）后以 dataURL 存入 localStorage；空间不足时提示并回滚。
- 初次使用自带示例菜谱、冰箱、日用品和外出记录，可自行删除。
